spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dnd_artifact
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  hateoas:
    use-hal-as-default-json-media-type: false